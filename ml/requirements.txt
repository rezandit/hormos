# HormOS data/ML pipeline (runs locally/offline — NOT on Vercel).
# Pin ranges loosely for the hackathon; freeze exact versions before publishing
# for full reproducibility (see docs/benchmark-methodology.md).
pandas>=2.1,<3
numpy>=1.26,<3
pyarrow>=15,<19          # parquet export
requests>=2.31,<3        # NHANES download
scikit-learn>=1.4,<2     # correlations / later modeling
# Optional, later iterations:
# lightgbm>=4.1,<5
# jupyter>=1.0
